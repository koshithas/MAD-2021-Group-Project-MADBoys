<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techdecode.pizzaapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Burgur King"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PizzaApp">
        <activity android:name=".InsertDelivery">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".InsertPayment" >

        </activity>
        <activity android:name=".user_inside_food_item" />
        <activity android:name=".user_Home_Page" />
        <activity android:name=".add_product" />
        <activity android:name=".MainActivity">

        </activity>
        <activity
            android:name=".admin_food_list"
            android:label="Food List"
            android:parentActivityName=".add_product" />
    </application>

</manifest>